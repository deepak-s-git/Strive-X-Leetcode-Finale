#include<iostream>

int main(){
    std::cout << "Hey, Learner" << std::endl;
    return 0;
}


// 🧠 Explanation
// #include<iostream> → This includes the Input/Output Stream header, which allows us to use objects like std::cout and std::cin.
// (iostream only deals with standard input/output operations.)

// std::cout → Used to print output to the console.
// std::endl → Moves the cursor to a new line (similar to \n).

// The keyword std:: → This means we are using the standard namespace explicitly.
// It tells the compiler that cout belongs to the standard library (hence, std::cout).

// return 0; → Indicates that the program executed successfully.